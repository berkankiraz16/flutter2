name: mini_katalog
description: Mini Katalog Uygulaması - Flutter eğitim projesi. API'den ürün verisi, GridView, detay sayfası ve sepet simülasyonu.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
